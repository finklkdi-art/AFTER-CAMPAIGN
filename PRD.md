# AFTER CAMPAIGN — 화면 PRD (컨셉 B 확정)

> 캠페인 결과 리포트 자동화 도구의 **웹 UI 전면 재설계** 명세.
> 확정 디자인 방향은 **컨셉 B(광고 트렌디 · '성적표')** 이며, 시각 규격은 [design.md](design.md)를 따른다.
> 파이프라인·데이터 규칙은 [CLAUDE.md](CLAUDE.md) / `Project_Rule_Book_v0.md`가 상위 문서다. 충돌 시 그 두 문서를 우선한다.

---

## 1. 목적과 배경

- 흩어진 캠페인 자료(제안서·미디어믹스·데일리리포트·포스트바이·소재)를 올리면, **제안 목표 vs 실집행 실적을 대조**하고 **기간별·소재별 집행 로드맵**과 **Lesson-Learned·차기 전략**까지 담은 결과 리포트(PPTX)를 자동 생성한다.
- 이번 작업은 **기능 변경이 아니라 화면·시각 언어의 전면 재설계**다. 파이프라인·데이터 로직은 유지하고, 화면 구조와 디자인 시스템만 컨셉 B로 교체한다.
- 산출물 PPTX 디자인(`utils/report/theme.py`)은 이 문서의 범위가 **아니다**. 여기서 다루는 건 **작업 웹 화면**뿐이다.

## 2. 사용자 · 사용 맥락

- **주 사용자**: 광고 기획자(AE). 캠페인 종료 후 결과 보고를 준비한다.
- **환경**: 각자 로컬 PC에서 100% 로컬 웹(Local Web UI, Streamlit)으로 구동. 외부 DB·업로드·클라우드 저장 없음.
- **숙련도**: 도구는 처음이어도 광고 도메인은 능숙. 화면은 "지금 할 일" 하나에 집중시킨다.
- **정서적 목표**: 지루한 정산 도구가 아니라 **캠페인 '성적표'를 받아 보는 경험**. 위트 있고 트렌디하게(컨셉 B).

## 3. 정보 구조 · 내비게이션

상단 바 + 4단계 스텝퍼를 기본 골격으로 한다. 성과 대시보드(성적표)와 체크리스트는 상단 내비로 접근한다.

```
대문(랜딩)
 └ 시작하기
    → [1] 자료 업로드 ─(분석 다이얼로그)─▶ [2] 자료 검증 ─▶ [3] 리포트 생성 ─▶ [4] 미리보기 · 부분 수정 ─▶ 최종 추출
         상단 내비: 성적표(성과 대시보드) · 체크리스트 · 세션(파기/이어하기)
```

## 4. 화면 목록

| # | 화면 | 파일(현행) | 상태 |
|---|---|---|---|
| S0 | 대문 / 랜딩 | `web_main.py` · `theme_css.LANDING_CSS` | 재설계 |
| S1 | 자료 업로드 (Step 1) | `dashboard/step1_upload.py` | 재설계 |
| S1d | 분석 진행 다이얼로그 | `step1_upload._analyze_dialog` | 재설계 |
| S2 | 자료 검증 · 거시 검증 (Step 2) | `dashboard/step2_*.py` | 재설계 |
| S3 | 리포트 생성 (Step 3) | `dashboard/step3_generate.py` | 재설계 |
| S4 | 미리보기 · 부분 리렌더링 (Step 4) | `dashboard/step3_preview.py` · `utils/slide_preview.py` | 재설계 |
| S5 | 성과 대시보드 · '성적표' | 신규(시안 `Dashboard_B`) | 신규 |
| S6 | 체크리스트 | `utils/checklist_manager.py` 연동 | 재설계 |
| C* | 공통: 상단 바 · 스텝퍼 · 파기/이어하기 | `dashboard/shell.py` | 재설계 |

---

## 5. 화면별 상세

각 화면은 **목적 / 핵심 요소 / 상태(빈·로딩·경고·에러) / 데이터 출처 / 수용 기준**으로 기술한다. 카피는 전부 **해요체**, **이모지 금지**, 아이콘은 **Lucide**(인라인 SVG).

### S0. 대문 / 랜딩
- **목적**: 도구의 정체성을 한 문장으로 전하고 시작시킨다.
- **핵심 요소**: 대형 워드마크 `AFTER CAMPAIGN`, 한 줄 카피("흩어진 캠페인 자료를 올리면, 결과 리포트를 대신 만들어 드려요."), 단일 CTA `시작하기`, 팀 배지(비즈니스 3본부 · AX).
- **상태**: 정적. 진입 애니메이션은 320ms 이내.
- **수용 기준**
  - [ ] 화면당 CTA는 하나(`시작하기`)뿐이다.
  - [ ] 문장이 절 중간에서 줄바꿈되지 않는다(`.kbr` 의미 단위 줄바꿈).

### S1. 자료 업로드 (Step 1)
- **목적**: 이기종 문서를 한 번에 받아 파이프라인에 태운다.
- **핵심 요소**
  - 대형 드롭존(끌어다 놓기/클릭), 지원 확장자 뱃지(PDF·XLSX·PPTX·DOCX).
  - **올린 파일 칩 목록**(접이식 금지 — 파일명 + 용량 그대로 노출).
  - **자동 인식한 역할** 카드: 제안서 · 미디어믹스 · 데일리리포트 · 포스트바이 · 소재, 그리고 `미확정`.
  - **캠페인 개요 입력**: 캠페인명 · 광고주 · 품목(폴더명 추론 금지, AE 직접 입력).
  - 단일 primary CTA `분석 시작`.
- **상태**
  - 빈 상태: 드롭존만. 파일 없으면 CTA 비활성.
  - 경고: 역할 미확정 문서가 있으면 해당 카드를 경고 톤으로 표시(조용히 넘기지 않는다).
- **데이터 출처**: `ACCEPTED` 확장자, `run_step1` 역할 추론, `Config/document_roles.json`.
- **수용 기준**
  - [ ] 이미지·영상 등 미지원 확장자는 메타데이터만 수집하고 파싱하지 않는다.
  - [ ] 같은 역할 문서가 여러 개여도 전부 보존(덮어쓰기 금지)하고, 다수 존재를 화면·체크리스트에 남긴다.
  - [ ] 캠페인명·광고주·품목은 사용자가 직접 확정한다(추론값은 후보로만).

### S1d. 분석 진행 다이얼로그
- **목적**: 업로드 실체화 + Stage 1 파싱 진행을 실시간으로 보여 준다.
- **핵심 요소**: 진행 바, **엔진이 실제 보고한 단계**만 번역한 상태 문구(`Config/progress_labels.json`), 완료 시 자동으로 Step 2로 이동.
- **상태**
  - 진행: 실측 기반 안내("보통 20~40초"). 타이머로 문구를 지어내지 않는다.
  - 에러: 파싱 실패 시 강제 종료하지 않고 로그(마지막 4000자)를 보여 주고 `닫기`.
- **수용 기준**
  - [ ] 진행 문구는 추측성 포장 없이 실제 단계만 노출한다.
  - [ ] 실패해도 파이프라인이 죽지 않고 사용자에게 원인과 복구 경로를 준다.

### S2. 자료 검증 · 거시(Macro) 검증 (Step 2)
- **목적**: 사람이 뼈대를 확정하는 Human-in-the-loop 단계. 검증 부담은 **수치 단위 → 거시 단위**로 낮춘다.
- **핵심 요소**
  - **거시 확정 항목**: 캠페인 개요 / 매체 종류 / 집행 기간·스케줄 / 소재 리스트 / 전략·목적.
  - **매체 토글 + 차트**: 매체별 예산 비중, 계획 vs 실집행(토글 변경 시 '적용' 없이 즉시 재렌더 — `st.fragment`).
  - **세부 수치 상세 보기**: 강제하지 않되 제거하지 않는다(상세 영역으로 보존).
  - 미확정 항목은 확정으로 승격하지 않고 체크리스트에 `미확인`으로 남긴다.
- **상태**
  - 경고: 캠페인 총계와 매체별 합계 불일치 시 한쪽을 고르지 않고 **둘 다** 제시.
  - 빈: 선택 매체에 값이 없으면 차트를 비우고 '데이터 없음'을 명시(0으로 채우지 않는다).
- **데이터 출처**: `CampaignDataset`, `charts.media_frame/panel`, `theme.grade`(달성률 등급).
- **수용 기준**
  - [ ] 값이 없는 매체는 차트에서 빼되 화면·체크리스트에 제외 사실을 남긴다.
  - [ ] 증감(delta)은 **계획 대비**로 표기한다(달성률을 delta로 넣지 않는다).
  - [ ] 접이식 박스로 본문을 숨기지 않는다.

### S3. 리포트 생성 (Step 3)
- **목적**: 검증된 지식으로 PPTX 덱을 생성한다.
- **핵심 요소**: 생성 트리거(가장 강한 단일 primary), 진행 표시, 생성 결과 요약(슬라이드 수·포함 매체·경고 건수).
- **상태**: 생성 중 진행 표시 / 실패 시 원인 노출.
- **데이터 출처**: 인메모리 `CampaignKnowledge`, `utils/report/renderer`.
- **수용 기준**
  - [ ] 산출물 1페이지는 `[Checklist]` 슬라이드다(S6 연동).
  - [ ] 파일명 규칙 `YYMMDD_품목_자료명_v0_Cheil`을 따른다.

### S4. 미리보기 · 부분 리렌더링 (Step 4)
- **목적**: 생성된 덱을 슬라이드로 확인하고, 자유 서술형 지시로 **해당 블록만** 다시 만든다.
- **핵심 요소**
  - 슬라이드 썸네일 그리드(선택 상태 표시).
  - 미리보기 + **문안 수정 패널**(자유 지시 입력, 지시 해석은 Claude API — 1장 텍스트 + 지시문만 전송, 전송 전 화면에 그대로 보여 주고 실행 확인).
  - **블록 단위 재생성**(`block_id` 레지스트리, 파싱 재실행 없음 — 세션 캐시 `CampaignDataset` 재사용).
  - 최종 추출/다운로드(화면에서 가장 강한 단일 액션).
- **상태**
  - API 키 없음/실패: 프리셋(규칙 기반)으로 자동 강등, 전 기능 동작.
  - 재렌더: 해당 블록만 스피너, 나머지는 그대로.
- **데이터 출처**: `slide_preview`, `utils/report/revise`, Claude API 어댑터.
- **수용 기준**
  - [ ] API 전송 대상은 수정 대상 슬라이드 1장 텍스트 + 지시문으로 한정하고, 전송 직전 사용자 확인을 받는다.
  - [ ] API 사용 사실·건수를 체크리스트에 기재한다.
  - [ ] 부분 리렌더는 파싱을 다시 하지 않는다.

### S5. 성과 대시보드 · '성적표' (신규)
- **목적**: 캠페인 결과를 한 화면에 담는 **컨셉 B의 대표 화면**. (시안: `Dashboard_B`)
- **핵심 요소**
  - 히어로: 캠페인 개요 + **목표 달성률(등급 스탬프)**.
  - KPI 타일: 총 노출 · 클릭·CTR · 전환 · 실집행 예산(계획 대비 증감).
  - 매체별 예산 비중(스택 바), 계획 vs 실집행(그룹 막대).
  - 기간×소재 **집행 로드맵**(타임라인).
  - **Lesson-Learned**(잘한 것 / 아쉬운 것 / 다음 전략) 카드.
- **상태**: 데이터 부분 결손 시 해당 위젯만 '데이터 없음' 처리, 나머지는 유지.
- **데이터 출처**: `charts.campaign_actuals/totals_row`, 달성률 등급, insight 엔진(`utils/insight`).
- **수용 기준**
  - [ ] 모든 수치에 근거·출처(`파일명:시트명`)를 병기할 수 있다.
  - [ ] 큰 수는 억·만 단위로 접고 정확값은 툴팁에 병기한다.

### S6. 체크리스트
- **목적**: 보고 전에 사람이 더블 체크할 예외를 모은다.
- **핵심 요소(필수 기재 대상)**: 인풋 누락/파싱 실패 · 역할 미확정/저신뢰 · 동일 역할 다수 · 제안 vs 실집행 기간 불일치 · 매체 드랍 의심 · 수치 이상치 · KPI 명칭·단위 불일치 · **AE 미확인 통과 항목** · **Claude API 생성·수정 건수**.
- **수용 기준**
  - [ ] 조용한 소실이 없다 — 보존하지 못한 것은 반드시 여기에 남는다.

### C*. 공통 요소
- **상단 바**: 워드마크(로고 버튼) · 현재 캠페인 칩 · 세션 정보 · 파기 버튼.
- **스텝퍼**: 1 업로드 · 2 검증 · 3 리포트 · 4 미리보기. 완료 단계는 체크로.
- **파기 / 이어하기**: 세션 자산 파기(로컬), 스냅샷 이어하기(감사 목적 스냅샷은 유지).

---

## 6. 비기능 요건

- **보안·로컬 전용**: 외부 DB·업로드·텔레메트리 금지. 유일한 예외는 Claude API(S4, 최소 전송 + 사용자 확인). Streamlit은 `localhost` 바인딩.
- **폰트(구현 시 필수 주의)**: 컨셉 B 폰트(Black Han Sans · Bricolage Grotesque · Gothic A1)는 **Google Fonts CDN으로 로드하면 안 된다**(rule 1 외부 요청 금지). **로컬 번들 `@font-face`(local()/동봉 파일)** 로 제공하고, 미설치 시 시스템 폰트로 조용히 강등한다. (아트보드 시안에서만 CDN 링크를 썼고, 실제 앱에는 번들이 필요하다.) 상세는 design.md 3장.
- **인코딩**: 모든 파일 I/O는 `UTF-8` 강제.
- **이식성**: 절대경로를 데이터에 저장하지 않는다(상대경로). 운영 중 바뀔 값은 `Config/*.json`으로 외부화.
- **성능**: 병목은 렌더가 아니라 파싱. 부분 리렌더는 세션 캐시 재사용.
- **접근성**: 본문 대비 AA, 히트 타깃 ≥ 44px, 포커스 링 명확. 색만으로 의미를 전달하지 않는다(등급은 라벨 병기).
- **카피**: 화면은 해요체, 이모지 금지, 절 중간 줄바꿈 금지(`.kbr`). 숫자는 `tabular-nums`.

## 7. 데이터 · 상태 흐름

- Stage 간 전달은 인메모리 `CampaignKnowledge` 객체. 파일 왕복은 감사 스냅샷용.
- 직렬화는 `to_dict()`/`from_dict()` 대칭 유지. `NaN`/`Infinity` 금지(`None` 정규화).
- 슬라이드 블록은 `block_id` 레지스트리로 독립 재생성.

## 8. 범위 밖 · 향후

- 산출물 PPTX 테마(별도 시스템).
- 다크 모드(design.md에 토큰 자리만 마련, 이번 릴리스 비활성).
- 폴더 선택·이어하기 동선 복원(구현부는 유지, 화면 노출은 후속).
