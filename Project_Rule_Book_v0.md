# 광고 캠페인 결과 리포트 자동화 솔루션
## Project Rule Book v2.0

> **개정 이력**
> - v0.1 (2026.08.20) 초안 작성 — 4개 핵심 섹션 확정
> - **v2.0 (2026.09.03) 개정** — 프론트엔드 리빌딩 및 외부 연결 정책 변경
>   - **1.4 Claude API 예외 조항 신설** — '외부 API 호출 불가'의 유일한 예외.
>     최소 전송·사전 고지·키 관리·강등 동작·감사 5개 조건부 허용
>   - 인풋 경로에 **업로드 방식 추가** (1.2) — 폴더 선택은 병행 유지
>   - **2.3 거시(Macro) 검증 원칙 신설** — Stage 1.5 검증 수준을 수치에서 뼈대로 완화.
>     캠페인 개요는 AE 직접 입력으로 고정, 미확인 항목은 Checklist 기재
>   - 파이프라인에 **Stage 0(업로드) / Stage 4.5(부분 리렌더링)** 추가 (4장)
>   - 웹 UI 디자인 테마 '성적표' 신설 (8장) — 산출물 PPTX 디자인과 분리
> - **v1.0 (2026.08.30) 개정** — 실제 캠페인 데이터 6건 검증 결과 반영
>   - 로컬 웹(Streamlit) 배포 방식 확정에 따른 1.1 개정
>   - 인풋 문서 역할 체계 7종 신설 (5장)
>   - 기간별/소재별 로드맵 스키마 신설 (6장)
>   - 데이터 보존 원칙 신설 (2.0) — 슬롯 덮어쓰기·행 절단 금지
>   - 파이프라인에 Stage 1.5(Human-in-the-loop) 반영
>   - 폴더 구조·스크립트명을 실제 구현에 맞게 현행화

**프로젝트 정의**
> 단순한 성과 측정(성적표)이 아니라, 캠페인 기획(제안서)과 실행(포스트바이)의 치열한 과정을 한눈에 보기 좋게 정리하는 '캠페인 역사서(아카이빙)'이자, 결과 데이터를 바탕으로 향후 브랜드가 나아가야 할 방향성과 개선점을 제시하는 '건설적인 전략 로드맵' 문서

**최종 산출물**
- 리포트 형식: PowerPoint (.pptx)
- 최적 대상: 광고주(클라이언트) 보고/아카이빙
- 핵심 역할: 캠페인 히스토리 누적 자산화 & 전략적 인사이트 도출
- **필수 구성 요소 3종**: ① 기간별/소재별 집행 로드맵 ② 성과 대조 ③ Lesson-Learned 및 차기 전략

---

## 1️⃣ 시스템 및 개발 환경 규칙 (System Focus)

### 1.1 작동 환경 (Deployment & Data Security)

> **⚠️ v3.0 (2026.09) 전면 개정 — 아래 '기본 원칙'을 대체함.**
> 동료 AE 의 즉시 체험을 위해 **무료 외부 호스팅 + URL 배포**로 전환. v1.0~v2.0 의
> '100% 로컬 구동 · 외부 전송 금지 · localhost 고정 · Claude API 만 예외' 조항은
> 모두 폐기됨. 상세는 `claude.md` 1장(2026.09 개정)이 상위 문서.

**기본 원칙 (v3.0)**
- **외부 호스팅 배포**: Streamlit Community Cloud 등 무료 서버에 올려 URL 로 접근.
  localhost 고정·로컬 완결 전제는 해제. 바인딩·포트·TLS 는 플랫폼이 관리.
- **외부 API 전면 허용**: 챗봇·요약·이미지 품질을 위해 Claude·Gemini 등 사용 허용.
- **비밀 관리**: API 키는 환경변수 → `st.secrets` → `.env` 순. 저장소 커밋 금지
  (`.gitignore` 에 `secrets.toml`·`.env` 등재).
- **인증 없음**: 로그인·비밀번호 게이트 금지. 링크 클릭 시 바로 대시보드 진입.
- **세션 격리 + 완전 휘발성 (핵심 방어)**: 아래 1.1-A 참조. 인증이 없는 대신 이것이
  접속자 간 데이터 노출을 막는 유일한 방어선이므로 절대 약화 금지.
- **잔여 리스크 인지**: 잔존은 없애도 처리 중 제3자(호스팅·API) 경유 노출은 남음.
  민감 자산 취급 시 컴플라이언스 확인 권장 (판단은 운영자).

**1.1-A 세션 격리 + 완전 휘발성 (Zero Retention)**
- 접속자마다 `uuid4` 토큰 + 전용 샌드박스(`Output/Temp/Sessions/<token>/`). 타인 파일·작업 내역 상호 열람 불가.
- 업로드 원본은 리포트 단계 진입 시 파기(파싱 결과는 인메모리 보존). 생성 PPTX 는 다운로드 바이트 전달 직후 디스크 사본 파기.
- 무활동 세션은 TTL(기본 30분) 스윕으로 전체 파기. 상단 바 '세션 파기'로 즉시 전량 삭제.
- 조정값은 `Config/web_deploy.json` 외부화.
- **보안**: 민감한 클라이언트 데이터는 접근 제어 폴더 내에서만 관리

**인코딩 규칙**
- 모든 파일 처리: **UTF-8 강제** (BOM 제외)
- 이유: 한국어 텍스트(한글) 깨짐 방지, 다국어 지원 확장성 확보
- 모든 스크립트에서 명시적 인코딩 선언 필수

**배포 이식성 (각 동료 AE의 로컬 PC 기준)**
- 프로젝트 루트는 실행 파일 위치에서 **동적 해석**. 경로 하드코딩 금지
- 데이터에 **절대경로를 저장하지 말 것**. 캠페인 폴더 기준 **상대경로**로 직렬화
- 브랜드명·품목명 등을 코드에 하드코딩하지 말 것. 다른 광고주에게도 동작해야 함
- 운영 중 변경될 값(분류 키워드 등)은 `Config/` 아래 JSON으로 외부화하여 **코드 수정 없이 조정 가능**하게 할 것

### 1.2 폴더 구조 (Directory Architecture)

```
<프로젝트 루트>/
├── app.py                          # Streamlit 로컬 웹 진입점
├── requirements.txt
├── .streamlit/config.toml          # localhost 바인딩 등 로컬 전용 설정
│
├── Input/                          # 원본 데이터 폴더 (읽기 전용)
│   ├── Samples/
│   │   └── [캠페인명]/              # ⚠️ 하위 폴더 없이 문서가 평평하게 혼재
│   │       ├── 제안서.pdf
│   │       ├── 미디어 브리프.docx
│   │       ├── 미디어 믹스.xlsx
│   │       ├── 데일리리포트.xlsx
│   │       └── 포스트바이.pdf
│   └── _Template/                  # 공용 템플릿 및 기준 문서
│
├── Output/                         # 최종 산출물 폴더 (자동 생성)
│   ├── Temp/
│   │   ├── Uploads/[세션]/         # 🆕 업로드본 실체화 (Input 은 읽기 전용이므로)
│   │   └── ...                     # 검증 스냅샷 (감사용)
│   └── [YYMMDD_캠페인]/
│       └── YYMMDD_품목_결과리포트_v0_Cheil.pptx
│
├── Scripts/
│   ├── main.py                     # CLI 진입점 (폴백)
│   ├── data_scanning.py            # Stage 1
│   ├── stage_1_5_verification.py   # Stage 1.5 (CLI 폴백)
│   ├── models/                     # 데이터 모델
│   ├── ingest/                     # 🆕 Stage 0 — 업로드 실체화 · 진행률 콜백
│   ├── utils/                      # 파서·분류기·검증기
│   └── dashboard/                  # Stage 1.5 Streamlit UI
│       └── theme_css.py            # 🆕 '성적표' 웹 테마 (PPTX 디자인과 분리)
│
├── Config/                         # 외부화된 설정 (코드 수정 없이 조정)
│   └── document_roles.json         # 문서 역할 판정 키워드
│
├── claude.md                       # AI 실행 규칙 (매 세션 로드)
└── Project_Rule_Book_v0.md         # 본 문서 (사람이 읽는 상세 기준서)
```

**폴더 관리 규칙**
- Input 폴더는 **읽기 전용** (실수로 인한 원본 파일 손상 방지).
  🆕 **업로드본은 절대 `Input/` 아래에 쓰지 말 것.** `Output/Temp/Uploads/` 에만 실체화함
- **캠페인 폴더 내부에 하위 폴더 구조를 가정하지 말 것.** 실제 운영 데이터는 문서가 평평하게 섞여 있음
- Output 폴더는 **자동 생성** (기존 파일 덮어쓰기 금지, 버전 관리)
- 날짜/시간 스탬프 활용으로 캠페인별 이력 추적 가능하도록 설계

### 1.3 파일명 규칙 (File Naming Convention)

**필수 포맷**
```
YYMMDD_품목_자료명_v[VERSION]_Cheil.확장자
```

| 요소 | 규칙 | 예시 |
|------|------|------|
| **YYMMDD** | 생성 일자 (2자리 년도-월-일) | 260820 (2026년 8월 20일) |
| **품목** | 광고주 카테고리 | 가전, 식품, 스마트폰, 금융 |
| **자료명** | 문서 타입 명칭 | 결과리포트, 성과분석, 인사이트, 전략제안 |
| **v[VERSION]** | 버전 번호 (초안 v0부터) | v0, v1, v2_final |
| **_Cheil** | 대행사 명시 | 고정값 |
| **확장자** | 파일 타입 | .pptx, .xlsx, .pdf, .md |

**파일명 예시**
- ✅ `260820_냉장고_결과리포트_v0_Cheil.pptx`
- ✅ `260820_에어컨_성과분석_v1_Cheil.xlsx`
- ❌ `result_report_20260820.pptx` (미준수)
- ❌ `가전_리포트_최종.pptx` (날짜/버전 누락)

### 1.4 (폐기) Claude API 예외 조항

> **⚠️ v3.0 폐기.** 외부 API 가 전면 허용(1.1 v3.0)되면서 '유일한 예외'라는 이
> 조항의 전제가 사라짐. 아래 '허용 범위 금지 항목'은 무효. 다만 최소 전송·강등·
> Checklist 기재 같은 **운영 관행**은 `claude.md` 1.5 로 옮겨 유지함.
> (이력 보존을 위해 원문은 아래에 남김.)

**1) 허용 범위 — 이 목적 외 사용 금지**

| 구분 | 내용 |
|---|---|
| ✅ 허용 | 슬라이드 문안 수정 지시의 해석, 해당 슬라이드 문안 다듬기 |
| ❌ 금지 | 외부 DB·클라우드 저장소·텔레메트리, 타 LLM 제공사 API |
| ❌ 금지 | 원본 문서 파일 자체의 전송, 전체 데이터셋·일자별 원시 실적의 전송 |

**2) 최소 전송 원칙 (Minimum Disclosure)**

```
전송 대상 = 수정 대상 슬라이드 1장의 텍스트 + AE가 입력한 수정 지시문
```

- 캠페인 전체 데이터를 통째로 보내지 않음. **필요한 슬라이드 1장으로 한정**
- 전송 직전 **전송 내용 전문을 화면에 표시하고 AE의 명시적 실행 확인**을 받음
- 자동·백그라운드 전송 금지. AE가 버튼을 누른 건에 한해 1회 전송

**3) 키 관리**
- API 키를 **코드·저장소에 하드코딩 금지**
- 환경변수(`ANTHROPIC_API_KEY`) 또는 로컬 설정 파일에서 로드
- 키 파일은 **산출물·스냅샷·Output 폴더에 포함하지 말 것**

**4) 가용성 (Graceful Degradation)**
- **키가 없거나 네트워크·API 호출이 실패해도 파이프라인은 중단되지 않음**
- 프리셋 지시(규칙 기반)로 자동 강등하며, **API 없이도 전 기능이 동작**해야 함
- 이는 3.3(파싱 에러 시 강제 종료 금지)과 동일한 원칙임

**5) 감사(Audit)**
- API가 생성·수정한 문안은 **Checklist 슬라이드에 사용 사실과 건수를 기재**
- 광고주가 보는 본문에는 API 사용 각주를 넣지 말 것 (Checklist에만)

---

## 2️⃣ 데이터 처리 및 AE 업무 규칙 (User Experience Focus)

### 2.0 데이터 보존 원칙 (Non-Destructive) — **최우선 규칙**

> 🔴 **v1.0 신설.** 실제 데이터 검증에서 6개 캠페인 전부에 대량 소실이 확인되어 최상위 규칙으로 승격함.

**금지 사항**
| 금지 | 이유 | 대안 |
|---|---|---|
| **문서 슬롯 덮어쓰기** | 같은 역할 문서가 3~5개인 것이 정상. 대입하면 앞 문서가 소실됨 | 모든 문서를 리스트에 보존하고 역할은 태그로 부여 |
| **행(row) 임의 절단** | `[:3]` 등으로 자르면 일자별 데이터가 전멸함 | 원본 전량 보존, 화면 표시용 축약과 분리 |
| **조용한 소실** | 확인할 방법이 없어짐 | 보존 불가 시 **반드시 Checklist 기재** |

**검증 실측 (2026.08.30, 샘플 6개 캠페인)**
```
(에어컨) AI 무풍콤보★  : 데일리리포트 3,787행 + 미디어믹스 1,619행 소실
(VD) 무빙스타일        : 508행 소실
(냉장고) 키친핏 Max     : 451행 소실
(VD) OLED TV          : 445행 소실
(시스템에어컨) 인피니트  : 382행 소실
(세탁건조기) 워시타워    : 303행 소실
→ 6개 캠페인 100%에서 발생. 생존 문서는 한글 파일명 정렬 순서로 결정되어 비결정적이었음
```

### 2.1 데이터 참조 원칙 (Source Attribution)

> ⚠️ **v1.0 개정.** v0.1의 "포스트바이 우선" 고정 우선순위는 `claude.md`의 "강제적 우선순위를 두지 않음"과 모순되어 아래로 통합함.

**기본 원칙: 고정 우선순위를 두지 않고, 맥락으로 교차 인용한다**

| 데이터 성격 | 주 참조처 | 활용 방식 |
|---|---|---|
| **기획 의도 / 전략** | 제안서, 미디어 브리프 | 캠페인의 목표와 배경 서술 |
| **계획 예산 / 매체 구성** | 미디어 믹스 | 로드맵의 계획축 구성 |
| **일자별 집행 추이** | 데일리 리포트 | 로드맵의 시간축 구성 |
| **최종 확정 실적** | 포스트바이 / 결과보고서 | 성과 대조의 실적축 |
| **제품 메시지** | 세일즈 가이드 | 소구점 해석 |

**필수 준수 사항**
1. 교차 인용 시 **출처를 반드시 병기** (`파일명:시트명` 형식)
2. 문서 간 값이 다를 때 **임의로 하나를 택하지 말 것**. 양쪽을 함께 제시하고 Checklist에 불일치 기재
3. 추론 신뢰도가 낮은 값은 확정하지 말고 **후보로 제시 → 사람이 확정**

**충돌 처리 예시**

**상황**: 제안서 "노출 500만" vs 포스트바이 "실제 노출 450만"
```
→ 표기: 목표 500만 (제안서 p.4) / 실적 450만 (포스트바이 p.3) = 달성률 90%
→ 해석: "제안 대비 90% 노출 달성. 원인 분석 필요"
→ Checklist: 불일치 여부가 아닌 '달성률 90%의 원인'을 확인 요청
```

**상황**: 제안서 "타겟 20-30대" vs 실제 "40-50대 주요 도달"
```
→ 표기: 기획 타겟 20-30대 (제안서) / 실제 도달 40-50대 (포스트바이 p.12)
→ 해석: 기획 의도와 실제 반응층 괴리 발생
→ 인사이트: "타겟팅 재조정의 필요. 40-50대 메시지 강화 권고"
```

### 2.2 휴먼 터치 (Checklist) 강제

**AI 자동 판단 금지 원칙**
- AI는 **데이터 부재, 양식 불일치, 예외 상황을 자동으로 무시하거나 추정하지 않음**
- 모든 이슈는 **[Checklist] 슬라이드로 집계**되어 최종 PPT의 **1페이지**에 배치
- AE가 반드시 이슈를 확인하고 더블 체크 후 수정하도록 유도

**Checklist 대상 예외 사항**

| 예외 상황 | 검출 기준 | 기재 예시 |
|----------|---------|------------------|
| **인풋 파일 누락** | 필수 입력 파일 없음 | "⚠️ [입력 누락] 포스트바이 데이터 파일 없음" |
| 🆕 **문서 역할 미확정** | 추론 신뢰도 임계 미달 | "⚠️ [역할 미확정] '캠페인(합본).pdf'의 역할을 지정하세요" |
| 🆕 **동일 역할 다수** | 같은 역할 문서 2건 이상 | "ℹ️ [문서 다수] 미디어 믹스 3건 감지 — 대표 문서 확인 필요" |
| 🆕 **데이터 절단·소실** | 보존 실패 발생 | "❌ [데이터 소실] 데일리리포트 3,787행 미반영" |
| **양식 불일치** | KPI 정의 상이 | "⚠️ [양식오류] KPI명 '클릭수' vs 'CPC' 불일치" |
| **매체 드랍 의심** | 제안엔 있고 실적엔 없음 | "⚠️ [매체누락] 제안서의 '카카오톡'이 포스트바이에 미포함" |
| **수치 이상치** | 음수, 급변 | "⚠️ [데이터이상] 조회수 전월 대비 -95%" |
| **시간 범위 불일치** | 제안 기간 ≠ 실행 기간 | "⚠️ [기간오류] 제안: 8.1~31 / 실집행: 7.15~8.20" |
| **중복 계산 의심** | 매체별 합계 초과 | "⚠️ [중복확인] 총 도달이 매체별 합계 초과" |
| 🆕 **AE 미확인 통과** | 거시 검증에서 확인 안 함 | "⚠️ [미확인] 소재 리스트를 AE가 확인하지 않음" |
| 🆕 **API 문안 생성** | Claude API 호출 발생 | "ℹ️ [자동생성] Claude API로 수정한 문안 3건 — 문구 검증 필요" |

**우선순위 가이드**
```
1순위: 데이터 소실 / 필수 입력 누락 → 진행 전 반드시 해소
2순위: 수치 이상치, 역할 미확정    → AE 검증 필수
3순위: 양식 불일치                → 해석 가이드라인 제시
```

### 2.3 거시(Macro) 검증 원칙 🆕

> **v2.0 신설.** Stage 1.5의 기획자 개입 부담을 낮추기 위해 검증 수준을
> '마이크로한 수치 단위'에서 **'거시적 뼈대 단위'** 로 조정함.

**전환의 취지와 한계**

```
목적:   AE 가 매 수치를 확인하는 부담을 없애 실사용 가능한 동선을 만든다
비목적: 확인하지 않은 값을 '확정값'으로 승격시키는 것이 아니다
```

**AE 가 화면에서 확정하는 거시 항목**

| 항목 | 입력 방식 | 비고 |
|---|---|---|
| **캠페인 개요** | **AE 직접 입력** | 캠페인명·품목·광고주. 업로드 경로엔 폴더명이 없어 추론 불가 |
| 매체 종류 | 자동 추출 + 확인 | 목록 확인 및 제외 지정 |
| 집행 기간 / 스케줄 | 자동 추출 + 확인 | |
| 소재 리스트 | 자동 추출 + 확인 | |
| 캠페인 전략 · 목적 | 자동 추출 + 확인 | |

**필수 안전장치 (2.2 와 충돌하지 않기 위한 조건)**
1. 세부 수치 교정 UI 를 **삭제하지 말 것.** 접이식 '상세 보기'로 보존하여 필요한 AE 는 쓰게 함
2. **AE 가 확인하지 않은 항목은 확정 처리하지 말고 Checklist 에 '미확인'으로 기재**
3. 부담 경감이 **조용한 통과**로 이어지면 2.0(데이터 보존)·2.2(AI 자동 판단 금지) 위반임

### 2.4 수정의 용이성 (Editability)

**AI 산출물은 '초안(Draft)'**
- AI가 작성한 모든 텍스트는 최종본이 아님
- AE가 **"살을 붙이기 쉽도록"** 구조화하여 제공
- 명확한 모듈형(Bullet Point) 구조 필수

```
❌ 피해야 할 형식 (문단식, 수정 어려움)
"이번 캠페인은 타겟 오디언스 확대를 목표로 추진되었으며, SNS와
디스플레이 광고를 통해 20-30대에 도달하려는 전략을 취했다..."

✅ 추천하는 형식 (Bullet Point, 수정 용이)
[캠페인 목표]
• 타겟 오디언스 확대
• 20-30대 고소득층 도달

[주요 성과]
• 총 도달: 1,500만 (포스트바이 p.3)
• 클릭율: 2.3% (포스트바이 p.5)

[해석 및 평가]
• 도달 수는 목표 대비 [___] (AE 입력)
```

**템플릿 제공 방식**
- 각 섹션마다 **[AE 수정 포인트]** 표시
- 추천 문구는 *이탤릭체* (참고용)
- 핵심 수치는 **볼드체 + 데이터 출처** 명시 — 예: **1,500만 노출** (포스트바이 p.3)

---

## 3️⃣ 결과물 퀄리티 규칙 (Client Focus)

### 3.1 건설적 로드맵 제시

**금지 항목**
- ❌ 단순 수치 나열만으로 끝내기
- ❌ 근거 없는 긍정 포장 ("정말 좋은 성과를 거뒀습니다")
- ❌ 방어적 태도 ("여건상 어려웠습니다")
- ❌ 책임 회피성 표현

**실적 상황별 작성 방향**

| 실적 상황 | 작성 방향 | 예시 |
|----------|---------|------|
| **실적 초과** | 성공 요인 분석 + 차기 전략 | "노출 200% 달성. 원인: 20-30대 타겟팅 정확도 ↑. 차기: 동일 세그먼트 강화 권고" |
| **목표 달성** | 지속성 확보 방안 | "CTR 목표 2.0% 대비 100% 달성. 차기: 크리에이티브 A/B 테스트로 상향 추진" |
| **실적 저조** | 명확한 개선점 + 구체적 액션 | "CTR 1.2%로 목표 대비 60%. Lesson: [1] 타겟팅 재검토 [2] 크리에이티브 리뉴얼 [3] 배치 시간대 최적화" |

**Lesson Learned 프레임워크**
```
현재 상황: "실제 노출이 예상 대비 65% 수준"

원인 분석:
• 데이터 근거: 제안 목표 2,000만 (제안서 p.7) → 실적 1,300만 (포스트바이 p.3)
• 가설: 광고 소재의 클릭 유도력 부족
• 보조 가설: 타겟팅 범위 과다 vs 실제 반응층 편중

개선점:
1. [크리에이티브] 차기 캠페인에서 CTR 상위 5개 소재 리뉴얼
2. [타겟팅] '지역별 효율 분석' 데이터 기반 재설정
3. [예산] 초기 배분 시 보수적 예측치 적용 (목표×85% 기준)

차기 적용 예상효과:
→ 노출 상향: 1,300만 → 1,700만 (130% 개선)
```

### 3.2 데이터-해석 1:1 매칭

**규칙**
1. 모든 인사이트와 제안에 **데이터 근거 명시**
2. "~라고 판단됩니다" 형태의 근거 없는 주장 금지
3. **데이터 출처 명시** (포스트바이 p.X, 제안서 p.Y)

```
❌ "젊은 세대에게 더 강한 반응을 보였습니다."

✅ "20-30대 CTR 2.8%로 40-50대 1.1% 대비 2.5배 수준
   (포스트바이 p.12 연령대별 성과 분석)
   → 차기 캠페인은 20-30대 타겟팅에 예산 60% 집중 권고"
```

**데이터 인용 형식**
```
[주장 명제]
→ 근거 수치: [수치] (단위, 출처)
→ 비교 기준: 목표 대비 [O%] / 전월 대비 [O%]
→ 결론: [구체적 차기 액션]
```

**문체 규칙**

| 피해야 할 표현 | 추천하는 표현 |
|--------------|------------|
| "좀 더 나았어요" | "목표 대비 115% 달성" |
| "반응이 안 좋았어요" | "CTR 1.2%로 목표 2.0% 대비 60% 수준" |
| "아마도 그럴 거예요" | "A 가설(근거: 데이터X) vs B 가설(근거: 데이터Y) 검증 필요" |

**명사형 종결**
```
❌ "타겟팅을 재설정해야 합니다"   (동사 종결 금지)
✅ "타겟팅 재설정의 필요" / "타겟팅 재조정 권고"
```

---

## 4️⃣ 파이프라인 아키텍처

> ⚠️ **v1.0 개정.** v0.1의 4단계 구조에 **Stage 1.5 (Human-in-the-loop)** 를 추가하고, 실제 스크립트명으로 현행화함.
> ⚠️ **v2.0 개정.** **Stage 0(업로드)** 과 **Stage 4.5(부분 리렌더링)** 를 추가함.

```
┌──────────────────────────────────────────────────────┐
│ [입력] 업로드 또는 캠페인 폴더 — 문서 평평하게 혼재   │
└───────────────────────┬──────────────────────────────┘
                        ▼
        ┌───────────────────────────────┐
        │ Stage 0  업로드 실체화 🆕      │  ingest/
        │ • 다중 파일 업로드             │
        │ • Output/Temp/Uploads/ 에 저장 │
        │ • 진행률 콜백 → 로딩바         │
        └───────────────┬───────────────┘
                        ▼
        ┌───────────────────────────────┐
        │ Stage 1  데이터 스캔           │  data_scanning.py
        │ • 파일 스캔 / 파싱             │
        │ • 문서 역할 동적 추론          │
        │ • Campaign Knowledge 조립      │
        │ • 예외 → Checklist             │
        └───────────────┬───────────────┘
                        ▼  (인메모리 객체)
        ┌───────────────────────────────┐
        │ Stage 1.5  기획자 거시 검증 ★  │  app.py + dashboard/
        │ • Streamlit 로컬 웹 UI(성적표) │
        │ • 캠페인 개요 AE 직접 입력 🆕  │
        │ • 매체·기간·소재·전략 확인     │
        │ • 상세 수치 교정은 선택 사항   │
        │ • 미확인 항목 → Checklist      │
        └───────────────┬───────────────┘
                        ▼  (교정된 인메모리 객체)
        ┌───────────────────────────────┐
        │ Stage 2  KPI 대조 & 로드맵     │
        │ • 목표 vs 실적 매칭            │
        │ • 기간×소재×매체 로드맵 구성   │
        └───────────────┬───────────────┘
                        ▼
        ┌───────────────────────────────┐
        │ Stage 3  인사이트 생성          │
        │ • Lesson Learned / 차기 전략   │
        └───────────────┬───────────────┘
                        ▼
        ┌───────────────────────────────┐
        │ Stage 4  PPT 생성              │
        │ • 1p Checklist 슬라이드 필수   │
        │ • 파일명 규칙 적용             │
        └───────────────┬───────────────┘
                        ▼
        ┌───────────────────────────────┐
        │ Stage 4.5  부분 리렌더링 🆕    │  report/revise.py
        │ • 슬라이드 초안 미리보기       │
        │ • AE 수정 지시 입력            │
        │ • 지시 해석 → Claude API(1.4)  │
        │   또는 프리셋 규칙(강등 시)    │
        │ • 해당 block_id 만 재생성      │
        │   → 덱 재렌더 (파싱 재실행 X)  │
        └───────────────┬───────────────┘
                        ▼
              Output/ 저장
```

**모듈 독립성 원칙**
- 각 Stage는 `CampaignKnowledge`를 받아 `CampaignKnowledge`를 반환 (순수 함수적 계약)
- Stage 간 전달은 **인메모리 객체**. 파일 왕복은 감사 스냅샷 용도로만
- 직렬화는 **`to_dict()` / `from_dict()` 쌍이 반드시 대칭**일 것
  - 🔴 한쪽에만 필드를 추가하면 데이터가 조용히 소실됨 (v0.1 기간 중 실제 발생)
  - 라운드트립 테스트로 대칭성을 고정할 것
- JSON에 `NaN` / `Infinity` 등 **비표준 토큰을 남기지 말 것** (`None`으로 정규화)

**부분 리렌더링 원칙 🆕 (v2.0)**
- 병목은 렌더가 아니라 **파싱**임 (실측: 캠페인당 파싱 12~277초 / 렌더 1초 미만).
  따라서 부분 리렌더링은 **PPTX 내부를 XML 수술하지 않고**, 세션에 캐시된
  `CampaignDataset` 으로 해당 블록만 다시 만들어 **덱 전체를 재렌더**함
- 슬라이드 블록은 **`block_id` 로 식별**되며, 빌더는 레지스트리에 등록되어
  **한 블록만 독립적으로 재생성 가능**해야 함
- 재렌더는 원본 데이터를 변형하지 않음. 수정은 **표현(문안) 계층에서만** 일어남

---

## 5️⃣ 인풋 문서 역할 체계 🆕

> **v1.0 신설.** 실제 캠페인 폴더에는 문서가 5~7종 혼재하며, 제안서/포스트바이 2분류로는 담을 수 없음이 확인됨.

### 5.1 역할 정의

| 역할 키 | 문서 | 주요 기여 | 대표 파일명 패턴 |
|---|---|---|---|
| `proposal` | 제안서 / 기획안 | 기획 의도, 전략, 목표 KPI | 제안, 기획안, IMC |
| `media_brief` | 미디어 브리프 | 캠페인 개요, 타겟, 기간 | 브리프, brief |
| `media_mix` | 미디어 믹스 | **매체별 예산·기간** | 믹스, mix |
| `postbuy` | 포스트바이 / 결과보고서 | 최종 집행 실적 | 포스트바이, 결과보고, 결과리포트 |
| `daily_report` | 데일리 리포트 | **일자별 성과** | 데일리, daily |
| `sales_guide` | 제품 세일즈가이드 | 제품 소구점, 메시지 | 세일즈, 가이드 |
| `creative` | 크리에이티브 소재/시안 | 소재 리스트, 규격 | 크리에이티브, 소재, 시안 |
| `unknown` | 역할 미확정 | — | (판정 실패) |

### 5.2 판정 규칙
- **파일명이 1차 신호, 본문 텍스트가 2차 보조 신호**
- 판정 키워드는 `Config/document_roles.json`에 외부화. 코드 수정 없이 AE가 조정 가능
- 신뢰도가 임계값 미만이면 `unknown`으로 두고 **Checklist 기재 후 Stage 1.5에서 사람이 확정**
- **역할이 같은 문서가 여러 개인 것은 정상 상황.** 전부 보존하고 대표 문서만 별도 지정

### 5.3 파일 형식
- 지원 확장자: `.pdf` `.xlsx` `.pptx` `.docx`
- 그 외(이미지·영상 등)는 **메타데이터만 수집**하고 파싱 시도하지 말 것
- 표(table) 행 구조가 파서마다 다름에 유의 — xlsx는 dict 행, pdf는 list 행. 검증 로직은 **양쪽을 모두 처리**할 것

---

## 6️⃣ 기간별/소재별 로드맵 스키마 🆕

> **v1.0 신설.** 산출물의 핵심 구성 요소이나 v0.1에는 데이터 축이 없었음.

### 6.1 데이터 구조

```
RoadmapEntry (집행 단위)
├── 시작일 / 종료일        ← 데일리 리포트, 미디어 믹스
├── 매체                   ← 미디어 믹스
├── 소재ID                 ← 크리에이티브 / 소재 리스트
├── 목적                   ← 인지 / 고려 / 전환
├── 예산                   ← 미디어 믹스
└── 비고                   ← AE 입력

CreativeAsset (소재 단위)
├── 소재ID / 명칭
├── 규격                   ← 가로형 영상 / 세로형 숏폼 / 디지털 배너
├── 제작일
├── 집행 매체
└── 비고
```

### 6.2 구성 원칙
- **AI는 후보만 제시하고 확정하지 않음.** 데일리 리포트의 날짜축, 미디어 믹스의 매체×예산에서 후보를 추출하되, **Stage 1.5에서 기획자가 승인**해야 확정됨
- 추출 실패 시 빈 표를 제공하고 수동 입력을 받을 것. 추측으로 채우지 말 것
- 로드맵의 각 항목에 **출처 문서를 병기**할 것

---

## 7️⃣ 아카이빙 가치 (Archiving as Core Value)

**이 솔루션의 본질**
- 단순 "반복 작업 대체"가 아닌 **"브랜드 자산 누적 및 조직 학습"** 도구

```
시점 1: 개별 AE의 캠페인 실행 — "내 캠페인만 잘 돌면 됨"
   ↓ 솔루션 도입
시점 2: 캠페인 히스토리 누적 → 패턴 발견 ("가전은 20-30대, 식품은 40-50대에 강함")
   ↓ 6개월 누적
시점 3: 브랜드별 성공 노하우 축적 → 동료 AE의 신규 캠페인에 즉시 적용
   ↓ 1년 누적
시점 4: 조직의 전략적 자산화 → 데이터 기반 영업 제안, 신입 온보딩 가이드
```

**아카이빙 구조**
```
Archive/
├── 2026년/
│   ├── 01월/ 260131_가전_결과리포트_v0_Cheil.pptx
│   └── 연간 통합 리포트/
├── Brand_Archive/
│   └── 가전/ 성공사례_타겟팅_규칙.md, KPI_달성_패턴_분석.xlsx
└── Lessons_Learned/
    ├── 크리에이티브_효율성_분석.md
    └── 채널별_성과_벤치마크.xlsx
```

| 시기 | 효과 |
|------|------|
| **1-3개월** | 데이터 근거 마련 — "지난 2개월 데이터상 20-30대 CTR이 2배" |
| **3-6개월** | 패턴 발견 및 최적화 |
| **6-12개월** | 조직 레벨 전략화, 신입 AE 온보딩 교재 |
| **1년 이상** | 클라이언트 신뢰도 — "작년 3건 데이터에 기반한 제안" |

---

## 8️⃣ 웹 UI 디자인 테마 — '성적표' 🆕

> **v2.0 신설.** 웹 화면의 디자인 컨셉을 한국의 **성적표(Report Card)** 로 고정함.

### 8.1 적용 범위 — 엄격히 분리

```
✅ 적용:   Streamlit 웹 화면 (dashboard/theme_css.py)
❌ 미적용: 산출물 PPTX  (utils/report/theme.py — 기존 디자인 시스템 유지)
```

- 🔴 **웹 테마 코드가 `utils/report/theme.py` 를 참조하지 말 것.** 반대 방향도 금지.
  두 디자인 시스템은 물리적으로 분리하며, 폰트·bold 검증기로 PPTX 오염을 회귀 검증함

### 8.2 은유 매핑

| 성적표 요소 | 캠페인 리포트 대응 |
|---|---|
| 과목 | 매체 / 축(상품·타겟팅·소재·목적) |
| 점수 · 등급 | 목표 달성률 |
| 담임 소견 | Lesson Learned · 차기 전략 |
| 출결 특이사항 | Checklist 예외 항목 |
| 학기 | 캠페인 집행 기간 |

### 8.3 제약

- ⚠️ **웹폰트 CDN 링크 금지** (Google Fonts 등). 외부 요청은 1.1 위반임.
  `Fonts/` 파일을 base64 data URI 로 임베드하거나 로컬 폰트 스택만 사용할 것
- 성적표 은유가 **평가·질책의 뉘앙스로 읽히지 않도록** 문구는 3.1(건설적 로드맵)을 따름
- 업로드 안내 문구는 **실측에 근거**할 것 (v2.0 실측: pptx 0.00초/MB · pdf 0.46 · xlsx 5.01)

---

## 📋 적용 체크리스트

### 개발 단계
- [x] Input/Output 폴더 구조 확립
- [x] UTF-8 인코딩 강제
- [x] Stage 1 데이터 스캔 및 Checklist 생성
- [x] Stage 1.5 Streamlit 로컬 웹 UI
- [x] **데이터 보존 원칙(2.0) 코드 반영** — 슬롯 덮어쓰기 제거, 문서 레지스트리 보존
- [x] **문서 역할 체계 7종(5장) 구현** — `Config/document_roles.json` 외부화
- [x] **직렬화 대칭성 확보** — `to_dict`/`from_dict` 라운드트립 검증 (샘플 6건 통과)
- [x] **로드맵 스키마(6장) 구현** — `PlanLine` 기반 간트 슬라이드
- [x] **Stage 2·4 구현** — `utils/report/` 블록 빌더 + PPTX 렌더러
- [x] **Stage 3 구현** — `utils/insight/` 규칙 4축(매체·상품 효율 / 타겟팅 /
      목표 달성률 / 기간·Phase 추이) → Lesson Learned + 차기 전략 슬라이드.
      임계값·키워드는 `Config/insight_rules.json` 외부화
- [x] 로드맵 기간 결손 입력값의 슬라이드 반영 — `utils/roadmap_overlay.py`
      (매체·상품 포함 매칭, 미매칭 입력은 별도 막대로 보존, 멱등 처리)
- [x] 소재(creative) 축 전용 아카이빙 블록 — `소재별 집행 로드맵` + `소재별 집행 결과`
- [x] `CampaignDataset` 역직렬화(`from_dict`) — `utils/dataclass_io.py` +
      `utils/dataset_snapshot.py`, STEP 1 에 '이전 작업 이어하기' 복원 추가
      (샘플 6건 to_dict/from_dict 완전 일치, 결손 입력값까지 보존)
- [x] 로드맵 '목적' 축 — `■ 목적별 효율` 표의 퍼널 단계(Awareness/Consideration)를
      `purpose` 축으로 분리 + `목적별 집행 결과` 슬라이드. 미디어믹스에 목적 컬럼이
      있으면 로드맵 막대 라벨에 병기. 구현 중 결함 2건 수정:
      · '구분' 헤더 시트 → 목적 값 미수집으로 소재 2행 중복 집계
      · '소재' 중복 라벨 시트 → 목적 값이 소재로 오분류
- [x] 소재 이미지 썸네일 카드 — `utils/creative_assets.py` 로 캠페인 폴더 이미지를
      소재명에 자동 매칭(정규화 후 부분 일치), 미발견 소재는 자리표시자.
      비율 유지(contain) 배치 확인, 매칭 0건이면 슬라이드 미생성

### 품질 관리
- [x] 샘플 캠페인 6건 파일럿 검증
- [x] 데이터 소실 0건 확인 — 축 분리 후 매체별 Total 합 = 캠페인 Total
      (샘플 6건 차이 0원), 소계·평균 행 혼입 0건, 일자 오매핑 0건
- [x] Checklist 슬라이드 생성 정확도 검증 — 1페이지 고정, 자동 도출·제외 사유 기재
- [x] 타 광고주 폴더에서의 범용성 검증 — 가상 광고주(정수기/웨이브·왓챠·당근마켓·토스)로
      Stage 1~4 전 구간 검증. 역할 추론·매체별 합계(차이 0원)·인사이트 4축 전부 정상,
      기존 샘플 고유명 유입 0건. 과정에서 발견한 결함 2건 수정:
      · 미디어믹스 메타 라벨이 한글 전용이라 영문 표기 파일의 광고주·캠페인명 미추출
      · 테스트 스크립트 2건의 절대경로 하드코딩
- [x] `Examples/` 열기 실패 원인 확인 — xlsx 77건 전부 NASCA DRM 암호화
      (헤더 `<## NASCA DRM FILE - VER1.00 ##>`). 만들어져 있으나 호출되지 않던
      `utils/drm_check.py` 를 파서 4개 진입점에 연결해 한국어 안내로 대체
- [ ] `Examples/` 캠페인 문서보안 해제 후 재검증 (해제는 사내 절차 필요)

### v2.0 리빌딩 (2026.09.03~)
- [x] **0단계** xlsx 핸들 재사용 — 시트마다 워크북을 재파싱하던 결함 수정.
      `sheet_utils.read_sheet()` 도입, 파서 3곳 + `file_parser` 연결.
      **실측 xlsx 168.5초 → 18.2초 (9.3배)**, 최악 파일 107.8→8.1초.
      핸들을 try/finally 로 닫아 업로드본 삭제가 막히지 않게 함
- [x] **1단계** 업로드 인제스트 (`ingest/workspace.py` · `progress.py`).
      `Output/Temp/Uploads/<세션>/` 실체화 → 기존 폴더 파이프라인 그대로 사용.
      동일 파일명은 덮어쓰지 않고 `(1)` 로 보존. `run_step1(on_progress=)` 로 로딩바 연결
- [x] **2단계** '성적표' 웹 테마 (`dashboard/theme_css.py`) — 갱지·괘선·등급 도장.
      OS 로컬 폰트만 사용(웹폰트 CDN 없음), `utils/report/theme.py` 미참조
- [x] **3단계** 거시 검증 화면 (`dashboard/step2_macro.py`) — 5개 뼈대 항목.
      캠페인 개요 AE 직접 입력(`advertiser` 필드 대칭 추가), 상세 검증은 접이식 보존,
      미확인 항목은 Checklist 에 자동 기재
- [x] **4단계** 슬라이드 블록 레지스트리(`BlockDef` · `block_id`) + 미리보기 화면.
      `rebuild_block()` / `replace_block()` — **부분 리렌더링 0.08~0.26초
      (전체 재실행 대비 35배)**. 재생성 실패 시 기존 슬라이드 유지
- [x] **5단계** Claude API 수정 어댑터 (`utils/report/revise.py`) + 챗봇 UI.
      Rule Book 1.4 의 5대 조건을 코드로 강제 (허용범위·최소전송·키관리·강등·감사).
      **추가 안전장치 2건**: (a) `summary` 는 강조 수치가 런으로 끼어들어 있어
      수정 대상에서 제외, (b) 응답에 새 수치가 나오거나 원본 수치가 사라지면
      `numeric_drift()` 가 채택을 거부하고 프리셋으로 강등.
      키 없음 / 통신 실패 / 정책 거절 / JSON 불량 / 줄 수 불일치 5개 경로 실측 검증.
      전송 확인 게이트가 실제 API 호출 전에 게이팅. 회귀 검증에서 산출물 동일

**리빌딩 회귀 검증 (샘플 6건)**
```
매체별 Total 합 = 캠페인 Total   차이 0원 (4개 지표)
축 분리 / 소계·평균 혼입          위반 0건
PPTX 기하 넘침 / 허용 외 폰트     0건
직렬화 라운드트립                 리프값 66,576개 전량 일치, NaN 0건
렌더 표 합계                     일치 34열 / 의도된 절단 2열 / 불일치 0
산출물                           리빌딩 착수 전과 완전 동일
전체 파이프라인                   544초 → 90초 (6.0배)
```

### 런칭 후
- [ ] 동료 AE 배포 및 피드백 수집
- [ ] 월 1회 Rule Book 검토
- [ ] 아카이빙 데이터 정기 통합 분석

---

## 📚 참고 문서

**향후 작성 예정**
1. `AE_User_Guide.md` — AE 대상 사용 매뉴얼
2. `Technical_Specification.md` — 기술 스택 및 구현 세부사항

**관련 문서**
- `claude.md` — AI 실행 규칙 (매 세션 로드). 본 문서와 충돌 시 `claude.md` 우선
- `Data_Inventory.md` — 실제 캠페인 데이터 구조 조사 결과

---

**최종 검토 상태**: ✅ v2.0 개정 + 리빌딩 0~5단계 구현 완료 (2026.09.03).
샘플 6건 회귀 검증 통과 — 산출물은 리빌딩 착수 전과 완전 동일, 파이프라인 6.0배 가속
**다음 단계**: 동료 AE 배포 및 피드백 수집
