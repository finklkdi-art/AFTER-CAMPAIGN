# 광고 캠페인 결과 리포트 자동화 솔루션 - 의존성
# 외부 호스팅(Streamlit Community Cloud 등) 배포 전제 (2026.09 개정)

# --- 웹 UI ---
streamlit>=1.28.0
altair>=5.0.0           # dashboard/charts.py 시각화 (streamlit 내장 의존성이지만 명시)

# --- 데이터 처리 ---
pandas>=1.5.0

# --- 파일 파서 (utils/file_parser.py) ---
openpyxl>=3.1.0         # .xlsx
pdfplumber>=0.10.0      # .pdf
python-pptx>=0.6.21     # .pptx
python-docx>=1.1.0      # .docx
lxml>=4.9.0             # pptx/docx 내부 XML 처리 (명시 — 누락 시 파싱 실패)
chardet>=5.0.0           # utils/encoder.py 인코딩 자동 감지 (필수 — 앱 기동에 직결)

# --- 게재 보고(placement_report) 이미지 추출 ---
pypdfium2>=4.0.0        # PDF 페이지 렌더링
Pillow>=10.0.0          # pypdfium2 의 to_pil() 변환에 필요

# --- Claude API 어댑터 (claude.md 1.5 — 문안 수정·요약) ---
# 키 미설정 시에도 파이프라인은 프리셋으로 강등 동작합니다.
anthropic>=0.40.0

# --- MCP 서버: Lucide Icons 아이콘 검색 API ---
httpx>=0.24.0           # 비동기 HTTP 클라이언트 (아이콘 데이터 다운로드)

# ── 배포 메모 (Streamlit Community Cloud) ────────────────────────────────
# 이 파일이 바뀌면 컨테이너가 의존성부터 다시 설치하며 **프로세스를 새로
# 띄운다.** 그게 중요한 이유:
#   Streamlit 은 새 커밋을 받아도 메인 스크립트(web_main.py)만 다시 실행하고,
#   import 된 모듈(dashboard/theme_css.py 등)은 기동 시 sys.modules 에 캐시된
#   객체를 계속 쓴다. 그래서 모듈 안의 상수(HERO_HTML·_BASE_CSS 같은 CSS/마크업)를
#   고쳐 push 해도 화면에 반영되지 않고, 'Manage app → Reboot app' 이 필요하다.
#   이 메모를 건드려 커밋하는 것은 그 Reboot 을 코드로 대신하는 수단이다.
# 마지막 강제 재기동: 2026-09-08 (UI 전면 개편 배포)
