# 광고 캠페인 결과 리포트 자동화 솔루션 - 프로젝트 가이드

> 본 문서는 **AI가 매 세션 읽는 실행 규칙**임. 상세 기준과 배경은 `Project_Rule_Book_v0.md` 참조.
> 두 문서가 충돌할 경우 본 문서를 우선함.

## 프로젝트 정체성 (Core Identity)
- 단순 '포스트바이(매체 관점)' 성과 측정을 넘어, 캠페인 기간의 **스케줄·크리에이티브(소재 제안/제작) 히스토리 등 커뮤니케이션 활동 전반을 아카이빙**하는 리포트 자동화 도구임.
- 기획과 실행 결과를 종합해 캠페인 활동을 한눈에 담고, Lesson-Learned를 정리하여 향후 방향성을 제시하는 **'전략 로드맵'** 도출이 목적임.
- 산출물의 핵심 구성 요소는 다음 3가지이며, 하나라도 누락 시 미완성으로 간주함.
  1. **기간별/소재별 광고 집행 로드맵** (매체 × 기간 × 소재 × 목적 × 예산)
  2. **성과 대조** (제안 목표 vs 실집행 실적)
  3. **Lesson-Learned 및 차기 전략**

---

## 1. 시스템 및 배포 원칙 (2026.09 전면 개정)

> **개정 배경**: 동료 AE 가 파이썬 설치 없이 URL 만으로 즉시 체험하도록, 기존
> '외부 API 극히 제한 · 100% 로컬 구동' 조항을 해제하고 **무료 외부 호스팅
> (Streamlit Community Cloud 등)** 배포로 전환함. 진입 마찰(로그인·비밀번호)을
> 두지 않는 대신, **세션 격리 + 완전 휘발성**을 핵심 방어선으로 삼음.

### 1.0 외부 API · 호스팅 (해제된 조항)
- **외부 API 전면 허용.** 챗봇·요약·이미지 품질을 위해 Claude·Gemini 등 외부
  LLM/생성 API 사용을 허용함. (구 '외부 전송 금지'는 폐기.)
- **외부 호스팅 배포.** localhost 전용·로컬 구동 전제는 해제. Streamlit
  Community Cloud 등 무료 서버에 올려 URL 로 접근함. 바인딩·포트·TLS 는 플랫폼이 관리.
- **비밀 관리는 Secrets.** API 키는 `.env`(로컬) 또는 **`st.secrets`(호스팅)** 에서
  읽음. 키를 코드·저장소에 커밋 금지 (`.gitignore` 에 `secrets.toml`·`.env` 등재).

### 1.1 인증 없음 (Frictionless)
- **로그인·비밀번호 게이트를 만들지 말 것.** 링크 클릭 시 바로 대시보드로 진입.
- URL 이 외부에 노출되어도 무방하다는 전제. 대신 아래 1.2 를 반드시 지킴.

### 1.2 세션 격리 + 완전 휘발성 (핵심 방어 로직 — 절대 엄수)
- **세션 격리(Session Isolation).** 접속자마다 `uuid4` 토큰을 부여하고 전용
  샌드박스 루트(`Output/Temp/Sessions/<token>/`)를 만들어, **타인이 올린 파일·작업
  내역을 서로 절대 볼 수 없게** 함. 하위 모듈의 디스크 쓰기는 전부 이 루트에서 파생.
- **완전 휘발성(Zero Retention).** 업로드 원본·생성 PPTX 는 서버 디스크에
  무기한 잔존 금지.
  - 다운로드 버튼은 렌더 시점에 바이트를 응답에 실으므로, 생성 직후 디스크 사본 파기.
  - 리포트 단계 진입 시 업로드 원본 파기(파싱 결과는 인메모리 knowledge/dataset 에 보존).
  - 무활동 세션은 TTL(기본 30분) 스윕으로 샌드박스 전체 파기.
  - 상단 바의 '세션 파기'로 즉시 전량 삭제 가능.
- **RAM 보호.** 업로드 상한(50MB) 등으로 무료 서버 리소스를 지킴.

### 1.3 남는 리스크 (기록 — 판단은 사람 몫)
세션 격리·휘발성은 **서버 잔존**을 없앨 뿐, 처리 중 데이터가 **제3자(호스팅·API)
서버를 경유**하는 노출까지 없애지는 못함. 클라이언트 민감 자산 취급 시 컴플라이언스
확인을 권장하며, 공개 배포 여부는 운영자가 결정함.

### 1.4 공통
- 한국어 처리 오류 방지를 위해 **모든 인코딩은 `UTF-8` 강제**.
- 출력물 파일명 규칙: **`YYMMDD_품목_자료명_v0_Cheil`** 구조 엄수.
- 운영 중 바뀔 값(TTL·업로드 상한 등)은 `Config/web_deploy.json` 에 외부화.

### 1.1 배포 및 이식성 (각 AE 로컬 PC 배포 전제)
- **절대경로를 데이터에 저장하지 말 것.** 경로는 캠페인 폴더 기준 **상대경로**로 직렬화함.
- 프로젝트 루트는 실행 파일 위치에서 동적으로 해석 (`Path(__file__).parent`). 하드코딩 금지.
- 분류 키워드·카테고리 등 **운영 중 바뀔 값은 `Config/` 아래 JSON으로 외부화**하여, AE가 코드 수정 없이 조정 가능하게 할 것.
- 특정 브랜드·품목명을 코드에 하드코딩하지 말 것 (동료 AE의 다른 광고주에도 동작해야 함).

### 1.5 외부 API 운영 규칙 (전면 허용 하의 좋은 관행)

외부 API 는 이제 전면 허용되나(1.0), 아래 관행은 품질·신뢰·비용을 위해 유지함.

- **최소 전송 지향.** 필요 이상으로 원본 전체를 보내지 말 것. 문안 수정은
  대상 슬라이드 텍스트 + 지시문 수준으로 보내고, 전송 전 화면에 내용을 보여 주는
  UX 를 권장(강제는 아님). 불필요한 대량 전송은 비용·노출 모두에 불리.
- **키가 없거나 실패해도 파이프라인은 계속.** 규칙 기반 프리셋으로 자동
  강등(graceful degradation)하여 API 없이도 전 기능이 동작해야 함.
- API 가 생성·수정한 문안은 **Checklist 에 사용 사실·건수를 기재**.
- 광고주 보고 본문에는 API 사용 각주를 넣지 말 것 (Checklist 슬라이드에만).
- 키는 **환경변수 → `st.secrets` → `.env`** 순으로 읽음. 저장소 커밋 금지.

---

## 2. 인풋 문서 및 역할 체계

인풋은 두 경로로 들어옴. 어느 쪽이든 **이기종 문서가 평평하게(flat) 혼재**하며,
하위 폴더 구조를 가정하지 말 것.

| 경로 | 방식 | 비고 |
|---|---|---|
| **업로드** (기본) | 웹 화면에서 파일 다중 업로드 | 세션 작업 폴더(`Output/Temp/Uploads/`)에 실체화 후 동일 파이프라인 적용 |
| **폴더 선택** | `Input/` 하위 캠페인 폴더 지정 | 기존 동선 유지 |

- 업로드본을 `Input/` 아래에 쓰지 말 것 (`Input/`은 읽기 전용).
- **캠페인명·품목 등 핵심 개요는 폴더명에서 유추하지 않고 Stage 1.5에서 AE가 직접 입력**함
  (업로드 경로에는 폴더명이 없으므로 추론에 의존할 수 없음).

스크립트가 파일명과 내부 텍스트 패턴을 읽어 아래 역할을 **동적으로 추론**함.

| 역할 키 | 문서 | 주요 기여 |
|---|---|---|
| `proposal` | 제안서 / 기획안 | 기획 의도, 전략, 목표 KPI |
| `media_brief` | 미디어 브리프 | 캠페인 개요, 타겟, 기간 |
| `media_mix` | 미디어 믹스 | **매체별 예산·기간 (로드맵 원천)** |
| `postbuy` | 포스트바이 / 결과보고서 | 최종 집행 실적 |
| `daily_report` | 데일리 리포트 | **일자별 성과 (로드맵 시간축 원천)** |
| `sales_guide` | 제품 세일즈가이드 | 제품 소구점, 메시지 |
| `creative` | 크리에이티브 소재/시안 | 소재 리스트, 규격 |
| `unknown` | 역할 미확정 | Checklist 기재 후 사람이 지정 |

- 지원 확장자: `.pdf` `.xlsx` `.pptx` `.docx`. 그 외 확장자(이미지·영상 등)는 **메타데이터만 수집**하고 파싱 시도하지 말 것.
- 역할 판정 규칙은 `Config/document_roles.json`에 두고 코드에서 분리할 것.

---

## 3. 데이터 처리 원칙

### 3.1 데이터 보존 (Non-Destructive) — 최우선
- **문서 덮어쓰기 절대 금지.** 같은 역할로 판정된 문서가 여러 개여도 전부 보존할 것. 한 슬롯에 대입하여 앞 문서를 잃는 구조를 만들지 말 것.
- **행(row) 절단 금지.** `[:3]` 같은 임의 절단으로 원본을 버리지 말 것. 화면 표시용 축약과 저장용 원본을 분리할 것.
- 문서를 보존하지 못하는 상황이 발생하면 **반드시 Checklist에 기재**할 것. 조용한 소실은 최악의 실패임.

### 3.2 참조 우선순위 (Context-Aware)
- 강제적 우선순위를 두지 않음. 인풋 문서들의 텍스트 패턴과 맥락(Campaign Knowledge)을 스스로 파악하여, **기획 의도와 최종 집행 팩트를 적확하고 유연하게 교차 인용**할 것.
- 단, 교차 인용 시 **어느 문서에서 온 값인지 출처를 반드시 병기**할 것 (`파일명:시트명` 형식).
- 문서 간 값이 다를 때 임의로 하나를 택하지 말고, **양쪽 값을 함께 제시하고 Checklist에 불일치를 기재**할 것.

### 3.3 예외 상황 처리
- 파싱 에러 시 **강제 종료 금지.** 메모리에 기록하고 파이프라인을 계속 진행할 것.
- 문서 간 데이터 불일치, 누락 의심 시 **AI 임의 판단 및 삭제 금지.**
- 추론 신뢰도가 낮은 값은 확정하지 말고 **후보로 제시하고 사람이 확정**하게 할 것.

### 3.4 Checklist (휴먼 터치)
반드시 최종 PPT의 **1페이지를 `[Checklist]` 슬라이드**로 만들어 기획자가 더블 체크하도록 예외 사항을 기재할 것.

Checklist 필수 기재 대상:
- 인풋 파일 누락 / 파싱 실패
- **문서 역할 미확정(`unknown`) 또는 저신뢰 추론**
- **동일 역할 문서 다수 존재 (사람의 확인 필요)**
- 제안 vs 실집행 기간 불일치
- 매체 드랍 의심 (제안에 있으나 실적에 없음)
- 수치 이상치 (음수, 급변)
- KPI 명칭·단위 불일치
- 🆕 **AE가 확인하지 않고 통과시킨 항목** (거시 검증 전환에 따른 필수 기재 — 3.5)
- 🆕 **Claude API로 생성·수정한 문안 건수** (1.2)

### 3.5 거시(Macro) 검증 원칙 (2026.09.03 개정)

Stage 1.5에서 기획자에게 요구하는 검증 수준을 **수치 단위에서 뼈대 단위로 낮춤.**
검증 부담을 줄이는 것이 목적이며, **검증하지 않은 값을 확정값으로 승격시키는 것이 아님.**

**AE가 화면에서 확정하는 거시 항목**
- 캠페인 개요 (**캠페인명·품목·광고주 — AE 직접 입력**)
- 매체 종류 / 집행 기간 / 전체 스케줄
- 소재(크리에이티브) 리스트
- 캠페인 전략·목적

**필수 안전장치**
- 세부 수치 교정은 **강제하지 않되 UI에서 제거하지 말 것** (접이식 '상세 보기'로 보존).
- **AE가 확인하지 않은 항목은 확정으로 처리하지 말고 Checklist에 '미확인'으로 남길 것.**
  부담 경감이 조용한 통과로 이어지면 2.2(AI 자동 판단 금지) 위반임.

---

## 4. 파이프라인 구조

```
Stage 0    업로드 → 세션 작업 폴더 실체화 (진행률 표시)
Stage 1    데이터 스캔 → 역할 추론 → Campaign Knowledge(인메모리) 조립
Stage 1.5  Streamlit 로컬 웹 UI — 기획자 거시(Macro) 검증 (Human-in-the-loop)
Stage 2    KPI 대조 및 로드맵 구성
Stage 3    인사이트 / Lesson-Learned 생성
Stage 4    PPT 생성 → Output 저장
Stage 4.5  슬라이드 미리보기 → 부분 리렌더링 (지시 해석은 Claude API, 1.2 준수)
```

- Stage 간 전달은 **인메모리 `CampaignKnowledge` 객체**로 함. 파일 왕복은 감사(audit) 스냅샷 용도로만.
- **부분 리렌더링은 파싱을 다시 하지 않음.** 세션에 캐시된 `CampaignDataset`을 재사용해
  해당 슬라이드 블록만 다시 만들고 덱을 재렌더함 (병목은 렌더가 아니라 파싱임 — 실측 근거).
- 슬라이드 블록은 **`block_id`로 식별되는 레지스트리**로 관리해 한 블록만 독립 재생성 가능해야 함.
- 직렬화는 **`to_dict()` / `from_dict()` 쌍이 반드시 대칭**이어야 함. 한쪽에만 필드를 추가하면 데이터가 조용히 소실됨.
- JSON 저장 시 `NaN` / `Infinity` 등 **비표준 토큰을 남기지 말 것** (`None`으로 정규화).

---

## 5. 화면(웹 UI) 카피 원칙 (2026.09.07 신설)

본 절은 **웹 화면**에만 적용함. 산출물 PPTX 문안 규칙(6장)과 분리함.

- **해요체**로 작성함. 격식체(~합니다/~하십시오)와 단정형(~다)을 쓰지 않음.
- **문장 중간에서 줄이 바뀌지 않게 할 것.** 줄바꿈은 반드시 의미 단위(절·구)
  경계에서만 일어나야 함. 한국어는 어절이 붙어 있어 임의 지점에서 끊기면
  의미 전달이 급격히 나빠짐.
  - `word-break: keep-all` 은 **어절**만 지켜 줌. 절 중간 줄바꿈은 못 막음.
  - 한 덩어리로 읽혀야 할 구절은 `<span class="kbr">` 로 감쌀 것
    (`.kbr { display: inline-block }` — `theme_css.py` 제공).
  - 고정 줄바꿈이 필요하면 `<br>` 를 의미 경계에 둘 것.
  - 예) `<span class="kbr">기록을 한곳에 모아,</span><br>`
        `<span class="kbr">제안 목표와 실집행 실적을 대조하고</span>`
- **파란 채움 버튼의 글자는 예외 없이 흰색 + 볼드**임. Streamlit 이 라벨을
  `<p>`/`<div>` 로 감싸므로 버튼 자신뿐 아니라 자식 요소까지 색을 지정할 것.
- **접이식 박스(`st.expander`)로 본문을 숨기지 말 것.** 클릭해야 내용이
  나오는 UI 는 쓰지 않음. 필요한 정보는 처음부터 보이게 배치하고, 양이
  많으면 개수·요약을 먼저 보여 준 뒤 목록을 그대로 펼쳐 둘 것.

---

## 6. 텍스트 출력 및 톤앤매너
- 텍스트는 기획자가 수정하기 편하도록 **모듈형(Bullet Point)** 으로 작성.
- **명사형 종결** 및 비즈니스 화법 사용 ("~의 필요", "~ 권고").
- 데이터 없는 **추측성 긍정 포장 금지.** 팩트 기반으로 서술할 것.
- 모든 주장에 **근거 수치와 출처**를 병기할 것.
