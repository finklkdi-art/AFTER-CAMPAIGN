# AFTER CAMPAIGN — 디자인 시스템 (컨셉 B)

> 확정 방향 **컨셉 B(광고 트렌디 · '성적표')** 의 시각 규격 단일 소스.
> 화면 요구는 [PRD.md](PRD.md), 파이프라인·데이터 규칙은 [CLAUDE.md](CLAUDE.md)를 따른다.
> 실제 적용 파일은 `Scripts/dashboard/theme_css.py`(웹 화면 전용). 산출물 PPTX 테마(`utils/report/theme.py`)와는 **물리적으로 분리**한다.

---

## 1. 디자인 원칙

컨셉 B는 정산 도구가 아니라 **캠페인 성적표**다. 에디토리얼 / 네오브루탈 감성.

1. **큰 타이포가 주인공.** 헤드라인·숫자를 과감하게 키운다. 위계는 색이 아니라 크기·굵기로.
2. **두꺼운 잉크 보더 + 하드 섀도.** 부드러운 그림자 대신 `2px` 잉크 외곽선과 오프셋 하드 섀도로 카드가 종이 위에 얹힌 느낌.
3. **크림 페이퍼 + 잉크 + 소수의 강한 액센트.** 은은한 그라데이션·과한 채움을 피하고 평면 색 블록으로.
4. **위트 있는 카피, 차분한 데이터.** 헤더는 재치 있게("이건 잘했어요"), 수치는 정확·중립적으로.
5. **이모지 금지. 아이콘은 Lucide.** 장식 남발 금지 — 데이터 슬롭(불필요한 숫자·아이콘) 배제.

---

## 2. 컬러 토큰

CSS 커스텀 프로퍼티로 단일화한다. 네임스페이스 `--ac-*`(After Campaign).

```css
:root {
  /* 표면 */
  --ac-paper:      #F3EEE4;  /* 페이지 바닥 */
  --ac-paper-2:    #F8F4EC;  /* 입력/드롭존 등 살짝 밝은 면 */
  --ac-surface:    #F3EEE4;  /* 카드 면(페이퍼와 동일, 보더로 구분) */

  /* 잉크(텍스트·보더·라인 공용) */
  --ac-ink:        #17140F;  /* 본문·제목·보더 기본 */
  --ac-ink-70:     rgba(23,20,15,.70); /* 보조 텍스트 */
  --ac-ink-55:     rgba(23,20,15,.55); /* 캡션 */
  --ac-ink-30:     rgba(23,20,15,.30); /* 트랙/그리드 */

  /* 액센트 (기능색) */
  --ac-cobalt:     #2B2BF5;  /* 주 강조 · 실집행 · 확정 */
  --ac-coral:      #FF5A38;  /* 액션(CTA) · 경고·아쉬움 */
  --ac-lime:       #C6F24E;  /* 긍정·하이라이트 (항상 잉크 텍스트) */
  --ac-violet:     #8B7CF6;  /* 보조 계열(매체 구분) */
  --ac-amber:      #FFB84D;  /* 보조 계열(매체 구분) */

  /* 온-컬러(위에 얹는 글자색) */
  --ac-on-cobalt:  #FFFFFF;
  --ac-on-coral:   #FFFFFF;
  --ac-on-lime:    var(--ac-ink);
  --ac-on-amber:   var(--ac-ink);
}
```

**사용 규칙**
- **강조색은 기능에 묶는다**: 코발트=실집행/확정, 코랄=액션·경고, 라임=긍정. 장식용으로 흩뿌리지 않는다.
- **매체 구분 팔레트**(도넛/스택): `코발트 → 코랄 → 라임 → 바이올렛 → 앰버 → 잉크` 순으로, 상위 매체가 강한 색을 갖게.
- **다크 모드**: 이번 릴리스 비활성. 토큰 자리는 `:root[data-theme="dark"]`로 마련만 하고 값은 후속(잉크↔페이퍼 반전, 액센트는 명도만 소폭 상향).

## 3. 타이포그래피

세 패밀리. 1~3종 원칙 준수.

| 역할 | 패밀리 | 용도 |
|---|---|---|
| 국문 디스플레이 | **Black Han Sans** (단일 웨이트) | 히어로·섹션 타이틀·패널 제목 |
| 라틴/숫자 디스플레이 | **Bricolage Grotesque** (500–800) | 큰 숫자·영문 키커·KPI 값 |
| 본문 | **Gothic A1** (400/500/700/900) | 본문·라벨·버튼·표 |

```css
--ac-font-display-kr: "Black Han Sans", "Gothic A1", sans-serif; /* weight 400 고정 */
--ac-font-display:    "Bricolage Grotesque", system-ui, sans-serif;
--ac-font-body:       "Gothic A1", system-ui, sans-serif;
--ac-num:             "Bricolage Grotesque", ui-monospace, monospace; /* font-variant-numeric: tabular-nums */
```

> ⚠️ **구현 필수 — 폰트는 로컬 번들.** 실제 앱은 외부 요청 금지(CLAUDE.md 1)라 **Google Fonts CDN 링크를 쓰면 안 된다.** 세 폰트를 `Fonts/`에 동봉하고 `@font-face`(local() 또는 동봉 파일)로 등록한다. 미설치 시 시스템 폰트로 조용히 강등. (아트보드 시안은 CSP 예외로 CDN을 썼지만, 프로덕션에는 번들이 필요하다.)

**타입 스케일(데스크톱 기준)**

| 토큰 | px / line | 용도 |
|---|---|---|
| `display-1` | 74 / .98 | 히어로 (Black Han Sans) |
| `display-2` | 60 / 1.0 | 서브 히어로 |
| `title` | 30 / 1.05 | 섹션 타이틀 |
| `panel` | 22 / 1.1 | 패널 제목 |
| `kpi` | 44 / 1.0 | KPI 숫자 (Bricolage) |
| `body` | 15 / 1.55 | 본문·버튼 |
| `label` | 13 / 1.4 | 라벨·칩 |
| `caption` | 12 / 1.4 | 캡션·단위 |

- 숫자는 항상 `--ac-num` + `tabular-nums`로 자릿수 고정.
- 한국어: `word-break: keep-all` + 의미 단위 묶음 `.kbr { display:inline-block }`. 절 중간 줄바꿈 금지.

## 4. 스페이싱 · 지오메트리 · 모션

```css
:root {
  /* 간격 스케일 (4의 배수) */
  --ac-2: 8px;  --ac-3: 12px; --ac-4: 16px; --ac-5: 20px;
  --ac-6: 24px; --ac-8: 34px; --ac-10: 48px;

  /* 라운드 */
  --ac-r-s: 10px; --ac-r-m: 14px; --ac-r-l: 18px; --ac-r-xl: 22px; --ac-r-pill: 40px;

  /* 보더 — 컨셉 B의 핵심. 기본 2px 잉크 */
  --ac-bd: 2px solid var(--ac-ink);
  --ac-bd-3: 3px dashed var(--ac-ink); /* 드롭존 */

  /* 하드 섀도(오프셋, 블러 0) */
  --ac-sh-3: 3px 3px 0 var(--ac-ink);
  --ac-sh-4: 4px 4px 0 var(--ac-ink);
  --ac-sh-5: 5px 5px 0 var(--ac-ink);
  --ac-sh-6: 6px 6px 0 var(--ac-ink);
  --ac-sh-8: 8px 8px 0 var(--ac-ink);

  /* 모션 — 3단계 상한 */
  --ac-ease: cubic-bezier(.22,.61,.36,1);
  --ac-dur-fast: 120ms; --ac-dur: 200ms; --ac-dur-slow: 320ms;
}
```

- 카드/패널은 `border: var(--ac-bd)` + `box-shadow: var(--ac-sh-6)`가 기본형.
- 인터랙션 시 섀도를 줄이며 살짝 눌리는 정도만(바운스·패럴랙스 금지).
- 데스크톱 전용 시안. 콘텐츠 폭 1440, 좌우 패딩 48px.

## 5. 아이콘 (Lucide)

- **Lucide 인라인 SVG만** 사용. 이모지·딩벳 금지.
- 규격: 24 그리드, `stroke-width: 2`, `stroke-linecap/linejoin: round`, `stroke="currentColor"`(fill none). 크기 16/18/20/24.
- 구현: Lucide 아이콘을 **로컬 SVG 세트로 번들**(외부 요청 금지). 자주 쓰는 세트 — `layout-dashboard, upload-cloud, file-text, calendar, bar-chart-3, target, trending-up/down, circle-check, alert-triangle, image, lightbulb, arrow-right, chevron-down, search, bell, plus`.

## 6. 컴포넌트

각 컴포넌트는 컨셉 B 규격(2px 잉크 보더 + 하드 섀도 + 볼드 타이포)을 공유한다.

- **버튼**
  - *Primary(액션)*: 코랄 채움, 잉크 보더, `--ac-sh-5`, 글자 흰색·굵게, 국문은 Black Han Sans. (예: `분석 시작`, `리포트 만들기`)
  - *Secondary*: 페이퍼 채움 + 잉크 보더, 섀도 없음/약하게.
  - *Pill*: 라운드 `--ac-r-pill`, 내비 배지/상태에.
  - 규칙: **채움 버튼 글자는 대비 확보**(코발트·코랄 위 흰색 볼드, 라임·앰버 위 잉크). 한 화면의 최종 액션은 하나만 강하게.
- **입력 필드**: `--ac-paper-2` 배경 + `2px` 잉크 보더 + `--ac-r-m`, 높이 52px, 라벨은 Black Han Sans 소형. 포커스 시 코발트 보더.
- **카드 / 패널**: 페이퍼 + `--ac-bd` + `--ac-sh-6` + `--ac-r-xl`. 제목은 `panel`(Black Han Sans), 부제는 `label`(잉크 55%).
- **KPI 타일**: 키커(번호, Bricolage) + 재치 라벨(Black Han Sans) + 큰 값(`kpi`, Bricolage) + delta 칩(라임=긍정/앰버=주의, 잉크 보더 pill).
- **달성률 등급 배지**: 초과 달성/달성/근접/미달/부진. 색은 라임(우수)·앰버(주의)·코랄(부진) + 잉크 텍스트·보더. 반드시 **라벨 병기**(색만으로 판단 금지).
- **스탬프**(성적표 도장): 라임 채움, 잉크 보더, Black Han Sans, `rotate(9deg)`. 히어로의 달성률 강조 1곳에만.
- **스택 바(매체 비중)**: 가로 100%, 높이 70px, `2px` 잉크 외곽 + 세그먼트 사이 `2px` 잉크 구분선. 세그먼트 안 % 라벨(Bricolage). 매체 팔레트 순서 준수.
- **그룹 막대(계획 vs 실집행)**: 계획=페이퍼+잉크 보더(빈 막대), 실집행=코발트 채움. 눈이 실적으로 먼저 가게.
- **로드맵(기간×소재)**: 8주 그리드 트랙(잉크 30% 세로선), 소재별 바는 `2px` 잉크 보더 + 매체별 색, 라벨 인라인.
- **체크리스트 아이템**: 좌측 상태 아이콘(경고=앰버/정보=코발트) + 제목(볼드) + 설명. 조용한 소실 금지 — 미확인/미확정을 반드시 노출.
- **드롭존**: `--ac-bd-3`(3px 대시 잉크) + `--ac-paper-2`, 중앙 아이콘 배지(라임 채움 + 하드 섀도), 지원 확장자 pill.
- **역할 카드**: 아이콘 배지(매체 팔레트) + 역할명(Black Han Sans) + 설명 + 인식 체크(코발트 원). 미확정은 앰버 배지 + 잉크 상태.
- **상단 내비**: 워드마크 `AFTER CAMPAIGN®`(Bricolage 800) + 언더라인 활성 링크 + 우측 pill/CTA.
- **스텝퍼**: 원형 번호(활성=코발트 채움) + 라벨, 사이 잉크 라인. 완료는 체크.
- **표**: 잉크 헤어라인, 헤더는 볼드 소형, 숫자 우측 정렬 + tabular. 합계행은 약한 강조.
- **인사이트 카드(Lesson-Learned)**: 잘한 것=라임, 아쉬운 것=코랄(흰 글자), 다음=코발트(흰 글자). 큰 숫자 1개 + 한 문장.

## 7. 카피 · 톤

- **해요체**. 격식체(~합니다)·단정형(~다) 금지.
- **이모지 금지**(브랜드가 쓰지 않는다).
- 절 중간 줄바꿈 금지 — `.kbr`로 의미 단위 묶기, 필요 시 `<br>`는 절 경계에.
- 헤더는 위트("이건 잘했어요/아쉬웠어요", "자료, 여기 다 던져 주세요"), 수치·근거는 중립·정확. 모든 주장에 근거 수치·출처 병기(`파일명:시트명`).
- 큰 수는 억·만 단위로 접고 정확값은 툴팁에 병기.

## 8. 상태 · 접근성

- **상태**: 기본 / hover(섀도 소폭 축소) / active(더 눌림) / focus(코발트 보더 + 3px 링) / disabled(투명도 30%).
- **대비(AA)**
  - 잉크(#17140F) on 페이퍼: 통과(고대비).
  - 흰 글자 on 코발트(#2B2BF5): 통과. on 코랄(#FF5A38): **소형 텍스트 부적합** → 큰·굵은 텍스트에만, 소형은 잉크 텍스트 사용.
  - 라임/앰버 위 텍스트: **항상 잉크**(흰색 금지).
- **의미 전달**: 색 + 라벨/아이콘 병기(등급·경고를 색만으로 표시하지 않는다).
- **히트 타깃** ≥ 44px, 숫자 tabular.

## 9. Streamlit 적용 매핑 (`theme_css.py`)

현행은 토스+삼성블루 팔레트다. 컨셉 B로 전환 시:

- `_tokens()`를 위 `--ac-*` 토큰으로 교체(라운드·모션 사다리 포함). 색 상수 이름(`TEXT/TEXT_HI/…`, `BRAND`)은 호출부 20여 곳 호환을 위해 **별칭 유지**하고 값만 컨셉 B로 매핑(`BRAND → --ac-cobalt` 등).
- `st.button[kind="primary"]` = 코랄 채움 + 잉크 보더 + 하드 섀도, 라벨과 **자식까지** 흰색·볼드 지정(Streamlit이 라벨을 `<p>/<div>`로 감싸는 문제 그대로 적용).
- 입력·업로더·토글·탭·메트릭·데이터프레임 위젯 스타일을 컴포넌트 6장 규격으로 재작성.
- `FONT_FACES`는 **local()/동봉 파일 기반 @font-face**로 세 폰트 등록(CDN 금지). 미설치 강등 스택 유지.
- `charts.py`의 `SERIES`를 매체 팔레트(코발트→코랄→라임→바이올렛→앰버→잉크)로 교체, 축·범례는 잉크 계열.
- 사이드바 미사용(상단 바 체제) 유지.

## 10. 구현 체크리스트

- [ ] 세 폰트 로컬 번들 + `@font-face` 등록(외부 요청 0건 확인).
- [ ] Lucide 아이콘 로컬 SVG 세트 번들.
- [ ] `--ac-*` 토큰으로 `_tokens()` 교체 + 기존 색 상수 별칭 매핑.
- [ ] Primary 버튼 코랄+잉크보더+하드섀도, 글자 대비 규칙 반영.
- [ ] 컴포넌트 6장 전 항목을 위젯 CSS로 이식.
- [ ] 매체 팔레트 차트 반영, 등급 배지 라벨 병기.
- [ ] 해요체·이모지 금지·`.kbr`·tabular-nums 전 화면 점검.
- [ ] 대비 AA(코랄 위 소형 텍스트 예외) 점검.
